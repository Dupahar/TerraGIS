# TerraGIS Private Beta MSIX Signing Update Evidence

- Version tag: 1.0.0-beta.6
- Evidence timestamp: 2026-04-16
- Workspace: C:\Users\mahaj\Downloads\GIS\TerraGIS
- Candidate artifact: artifacts/private-beta/1.0.0-beta.6-20260414-012059

## Summary

This evidence note captures post-packaging remediation and verification updates for MSIX creation/signing scripts and artifacts.

## Script Hardening Completed

1. `scripts/create-and-sign-msix.ps1`
   - Replaced plaintext password parameter usage with `System.Security.SecureString` parameter type.
   - Removed unused variable assignment (`appImageParent`).
   - Added signature verification step after successful signing.
   - Added non-blocking handling for expected self-signed trust-chain warnings.

2. `scripts/sign-msix.ps1`
   - Added `System.Security.SecureString` certificate password parameter.
   - Removed hardcoded plaintext password in signing flow.
   - Added secure random password fallback generation for test-signing path.
   - Added transient in-memory password conversion for `signtool` invocation and memory cleanup.
   - Added non-blocking handling for expected self-signed trust-chain warnings.

## MSIX Packaging and Signing Outcome

- MSIX package path:
  - `artifacts/private-beta/1.0.0-beta.6-20260414-012059/msix-output/TerraGIS.Beta.msix`
- Test certificate output:
  - `artifacts/private-beta/1.0.0-beta.6-20260414-012059/msix-output/TerraGIS.Beta.Test.pfx`

Observed behavior during verification:
- `signtool sign` succeeds.
- `signtool verify` returns a trust-chain warning on self-signed cert (`chain terminated in a root certificate not trusted by trust provider`) unless root trust is installed.
- Scripts now complete successfully while reporting that warning explicitly.

## Remaining Work Before Store Private Audience Submission

1. Re-sign MSIX with production certificate matching Partner Center identity.
2. Complete clean-machine validation matrix (Win10 + 2x Win11) and record outcomes.
3. Submit signed package to Partner Center private audience.
4. Validate private-audience tester install/launch path.
